<%= bootstrap_form_for([@playlist, @playlist.comments.build]) do |f| %>

<%= f.hidden_field :user_id %>

<div class="field">
  <div class="control">
    <%= f.text_area :content, hide_label: true  %>
  </div>
</div>
<%= f.submit 'Leave a comment' %>
<% end %>