<% if @playlist.errors.any? %>
  <div id="error_explanation">
    <h4><%= pluralize(@playlist.errors.count, "error") %> - please try again!</h4>

    <ul>
      <% @playlist.errors.each do |error| %>
        <div><%= error.full_message %></div>
      <% end %>
    </ul>
  </div>
<% end %>